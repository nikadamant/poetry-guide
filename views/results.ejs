<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Poem Results</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Poem Search Results</h1>

  <% if (error) { %>
    <p style="color: red;"><%= error %></p>
  <% } else { %>
    <ul>
      <% poems.forEach(poem => { %>
        <li>
          <a href="/poem/<%= encodeURIComponent(poem.title) %>">
            <%= poem.title %> — <%= poem.author %>
          </a>
        </li>
      <% }) %>
    </ul>
  <% } %>

  <a href="/">⬅ Back</a>
</body>
</html>
